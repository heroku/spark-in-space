#!/usr/bin/env bash
# bin/sleep
# process just sleeps forever.
# good to use as a `console` process type, that you subsequently run `heroku run:inside console.1 bash` against.

 while true; do sleep 10; done;